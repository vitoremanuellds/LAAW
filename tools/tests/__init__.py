"""LAAW tasks CLI test suite.

Run from the repo root (or the installed .ai/workflow/ root):
    python3 -m unittest discover -s tools/tests -t tools -v

Run from tools/:
    python3 -m unittest discover -s tests -t . -v

Stdlib unittest only — no third-party dependencies, no network.
tools/tests/ is NOT shipped into projects by sync-workflow.py.
"""

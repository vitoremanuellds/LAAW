# t{N} — {name}

Description: {description}

In scope:
- <what this task covers — the outcome the human checks at plan approval>

Out of scope:
- <what this task explicitly does NOT do; name the task or where that work belongs>

Context: <what the implementer must know to start; name the .ai/context/ files to re-read>

Steps:
<!-- Implementation-ready: each step is a concrete action or a short pseudocode
     block the implementer can execute without re-designing. Pseudocode and
     well-defined micro-steps are encouraged; never final code. After plan
     approval a step's text is a record: only the checkbox may flip to [x];
     never reword, merge, or delete step text. -->
- [ ] <step 1>
- [ ] <step 2>

Validations:
- <real command or check that proves done, e.g. `npm test`, build, grep, manual check>

Context updates:
- <.ai/context/ file: exact change to make when this task finishes — or "none">

Notes:
<!-- blockers, decisions, validation results. Status lives in .ai/tasks/state.json — never in task files. -->
